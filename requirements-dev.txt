# Development & CI tooling — not needed to run the app.
# Installs the core app too, so `pip install -r requirements-dev.txt` gives a
# full dev/test environment (torch ML extras are separate: requirements-ml.txt).
-r requirements.txt

pytest==9.1.1
ruff==0.15.21

# Wraps PostgreSQL's own parser. tests/test_sql_schemas.py uses it to check the files in
# sql/ against the real grammar — including the RLS/policy statements that generic SQL
# parsers do not model — and to diff their columns against what data_manager writes.
pglast==8.4
